# EchoChat environment (systemd EnvironmentFile)
#
# Copy to: /etc/echochat/echochat.env
# Set permissions: chmod 600 /etc/echochat/echochat.env

# Prevent secrets from being written to server_config.json
ECHOCHAT_PERSIST_SECRETS=0

# Path to the EchoChat JSON config file (used by wsgi.py / janitor_runner.py)
ECHOCHAT_CONFIG=/opt/echochat/Echo-Chat-main/server_config.json

# Core secrets
SECRET_KEY=change_me_to_a_long_random_value
JWT_SECRET_KEY=change_me_to_a_different_long_random_value
DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/echo_db

# Redis (recommended for scale)
REDIS_URL=redis://127.0.0.1:6379/0

# Optional overrides
# ECHOCHAT_SOCKETIO_MESSAGE_QUEUE=redis://127.0.0.1:6379/0

# WebSockets (recommended): use Eventlet
ECHOCHAT_SOCKETIO_ASYNC=eventlet

# Gunicorn (only used by echochat-gunicorn.service)
ECHOCHAT_BIND=0.0.0.0:5000
ECHOCHAT_WORKERS=2

# GIFs
GIPHY_API_KEY=

# LiveKit (voice/video)
ECHOCHAT_LIVEKIT_ENABLED=0
LIVEKIT_API_URL=https://your-livekit-host
LIVEKIT_WS_URL=wss://your-livekit-host
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=

# SMTP (password reset)
ECHOCHAT_SMTP_ENABLED=0
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=EchoChat <no-reply@localhost>
SMTP_STARTTLS=1
SMTP_SSL=0
